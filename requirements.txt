# Core DPI engine: NO third-party dependencies.  Standard library only.
#
# The packages below are required ONLY by the optional AI analysis layer
# (`ai/`, `analyze_ai.py`).  Every existing tool -- main.py, main_simple.py,
# main_working.py, main_dpi.py, dpi_mt.py -- and `run_selftests.py` run
# without them.
#
# One SDK serves all three providers: Groq and Ollama both expose
# OpenAI-compatible /chat/completions endpoints, so no `groq` or `ollama`
# package is needed.
#
# Requires Python 3.10 or newer.

pydantic>=2.0      # schema definition and validation of LLM responses
openai>=1.40       # OpenAI-compatible client: OpenAI, Groq, and Ollama
